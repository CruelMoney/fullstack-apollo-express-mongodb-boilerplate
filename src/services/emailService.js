const sendVerificationEmail = async ({ email, token }) => {
  console.warn('Not implemented', { email, token });
};

const sendPasswordResetEmail = async ({ email, token }) => {
  console.warn('Not implemented', { email, token });
};

export { sendVerificationEmail, sendPasswordResetEmail };
